Title: Iteration 1 — Radar Chart Visualization

Description:
Add a radar chart to visualize the 6 scores returned by the insight engine.

Acceptance criteria:
- Integrate Chart.js (or similar) into `public/`.
- When insights return, render a radar chart with labeled axes and score values.
- Chart is responsive and matches the repo's visual style.

Notes:
Create `public/charts.js` and include it in `public/index.html`.